<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Cadence Talent | Building PDE Teams</title>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
<style>
  /* Reset & base */
  * { margin:0; padding:0; box-sizing:border-box; scroll-behavior: smooth; }
  body { font-family: 'Inter', sans-serif; color:#1F1F1F; line-height:1.6; transition: background 0.3s, color 0.3s; }
  a { color: inherit; text-decoration: none; }
  img { max-width: 100%; }

  /* Dark/Light Mode */
  body.light { background: #f9f9f9; color: #1F1F1F; }
  body.light header { background: linear-gradient(135deg, #4F92FF, #32D6A1); color: white; }
  body.light .card { background: white; box-shadow:0 5px 20px rgba(0,0,0,0.1); }
  body.dark { background: #1F1F1F; color: #f9f9f9; }
  body.dark header { background: #111; color: #fff; }
  body.dark .card { background: #222; box-shadow:0 5px 20px rgba(255,255,255,0.05); }

  /* Header & Nav */
  header { position: relative; padding: 20px 0; }
  nav { display:flex; justify-content: space-between; align-items:center; width:90%; max-width:1200px; margin:0 auto; }
  nav .logo { font-weight:700; font-size:1.5rem; cursor:pointer; }
  nav ul { display:flex; list-style:none; gap:20px; }
  nav ul li a { font-weight:600; cursor:pointer; transition:0.3s; }
  nav ul li a:hover { text-decoration:underline; }
  .toggle-mode { cursor:pointer; font-size:0.9rem; background:none; border:none; color:inherit; margin-left:20px; }

  /* Hero */
  .hero { display:flex; flex-direction:column; justify-content:center; align-items:center; text-align:center; min-height:90vh; padding:0 20px; transition: opacity 1s ease-out; opacity:0; }
  .hero.visible { opacity:1; }
  .hero h1 { font-size:3rem; margin-bottom:1rem; font-weight:700; }
  .hero p { font-size:1.25rem; margin-bottom:2rem; }
  .hero form { display:flex; flex-wrap:wrap; gap:10px; justify-content:center; transition: opacity 1s ease-out; }
  .hero input[type="email"] { padding:0.75rem 1rem; border-radius:5px; border:none; font-size:1rem; width:250px; }
  .hero button { padding:0.75rem 1.25rem; border:none; border-radius:5px; background:#1F1F1F; color:white; font-weight:600; cursor:pointer; transition:0.3s; }
  .hero button:hover { background:#333; }

  /* Section Styling */
  section { padding:80px 0; text-align:center; transition: all 1s ease-out; opacity:0; transform: translateY(30px); }
  section.visible { opacity:1; transform: translateY(0); }
  section h2 { font-size:2rem; margin-bottom:1rem; font-weight:700; }
  section p { font-size:1.1rem; max-width:700px; margin:0 auto 2rem; }

  /* Cards */
  .cards { display:flex; flex-wrap:wrap; justify-content:center; gap:20px; }
  .card { border-radius:10px; padding:20px; width:280px; transition:0.3s, transform 0.3s; cursor:pointer; }
  .card:hover { transform: translateY(-10px) scale(1.02); box-shadow:0 10px 25px rgba(0,0,0,0.15); }

  /* Contact Form */
  form.contact-form { display:flex; flex-direction: column; max-width:500px; margin:0 auto; gap:15px; }
  form.contact-form input, form.contact-form textarea { padding:0.75rem 1rem; border-radius:5px; border:1px solid #ccc; font-size:1rem; width:100%; }
  form.contact-form button { padding:0.75rem 1.25rem; border:none; border-radius:5px; background:#4F92FF; color:white; font-weight:600; cursor:pointer; transition:0.3s; }
  form.contact-form button:hover { background:#3670c0; }

  /* Footer */
  footer { padding:40px 0; text-align:center; font-size:0.9rem; color:#666; background:#f9f9f9; transition: background 0.3s, color 0.3s; }

  /* Responsive */
  @media(max-width:768px){ 
    .cards{flex-direction:column;align-items:center;} 
    nav ul{flex-direction:column; gap:10px;}
  }
</style>
</head>
<body class="light">

<!-- Header / Nav -->
<header>
  <nav>
    <div class="logo">Cadence Talent</div>
    <ul>
      <li><a href="#about">About</a></li>
      <li><a href="#services">Services</a></li>
      <li><a href="#why">Why Cadence</a></li>
      <li><a href="#contact">Contact</a></li>
      <button class="toggle-mode">🌙 Dark Mode</button>
    </ul>
  </nav>
  <div class="hero">
    <h1>Building the teams that build the future</h1>
    <p>Connecting world-class Engineering, Product, and Design talent with the companies that need them.</p>
    <form id="hero-form">
      <input type="email" placeholder="Enter your email" required>
      <button type="submit">Stay in the Loop</button>
    </form>
  </div>
</header>

<!-- About Section -->
<section id="about">
  <h2>About Cadence Talent</h2>
  <p>Cadence Talent specializes in recruiting across the entire Product Development Lifecycle — Engineering, Product, and Design. Whether you’re building your first hire or scaling an entire team, we connect you with the right talent, fast.</p>
  <div class="cards">
    <div class="card"><h3>Meta</h3><p>Executed high-impact PDE placements at Meta.</p></div>
    <div class="card"><h3>Cisco</h3><p>Placed leadership and key contributors across global teams.</p></div>
    <div class="card"><h3>PayPal</h3><p>Delivered top engineering and product talent to scale initiatives.</p></div>
  </div>
</section>

<!-- Services Section -->
<section id="services">
  <h2>Our Services</h2>
  <div class="cards">
    <div class="card"><h3>Executive Search</h3><p>Retained VP/Head/Director placements for tech and product organizations.</p></div>
    <div class="card"><h3>Team Buildouts</h3><p>IC and mid-level hiring for scaling tech teams quickly and efficiently.</p></div>
    <div class="card"><h3>Fractional Recruiting</h3><p>Embedded recruiting support for early-stage or growing teams.</p></div>
  </div>
</section>

<!-- Why Cadence Section -->
<section id="why">
  <h2>Why Cadence</h2>
  <div class="cards">
    <div class="card"><h3>Deep Experience</h3><p>Years of recruiting experience with top tech companies and startups.</p></div>
    <div class="card"><h3>PDE/SDLC Focus</h3><p>Specialized understanding of Product, Design, and Engineering workflows.</p></div>
    <div class="card"><h3>High-Touch Approach</h3><p>Direct collaboration with founders, execs, and hiring managers.</p></div>
    <div class="card"><h3>Flexible Engagement</h3><p>Retained, contingent, and fractional options to fit your hiring needs.</p></div>
  </div>
</section>

<!-- Contact Section -->
<section id="contact">
  <h2>Get in Touch</h2>
  <p>Cadence Talent is ready to help you build your next great team. Reach out today.</p>
  <form class="contact-form" id="contact-form">
    <input type="text" placeholder="Your Name" required>
    <input type="email" placeholder="Your Email" required>
    <input type="text" placeholder="Company (Optional)">
    <textarea placeholder="Message (Optional)" rows="4"></textarea>
    <button type="submit">Send Message</button>
  </form>
</section>

<!-- Footer -->
<footer>
  © 2025 Cadence Talent | <a href="#">Privacy Policy</a> | hello@cadencetalent.io
</footer>

<script>
  // Fade-in sections on scroll
  const sections = document.querySelectorAll('section, .hero');
  const observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if(entry.isIntersecting){ entry.target.classList.add('visible'); }
    });
  }, { threshold: 0.1 });
  sections.forEach(section => observer.observe(section));

  // Dark/Light mode toggle
  const toggleBtn = document.querySelector('.toggle-mode');
  toggleBtn.addEventListener('click', () => {
    document.body.classList.toggle('dark');
    document.body.classList.toggle('light');
    toggleBtn.textContent = document.body.classList.contains('dark') ? '☀️ Light Mode' : '🌙 Dark Mode';
  });

  // Simple form submit alerts
  document.getElementById('hero-form').addEventListener('submit', function(e){
    e.preventDefault(); alert('Thanks! You are subscribed.'); this.reset();
  });
  document.getElementById('contact-form').addEventListener('submit', function(e){
    e.preventDefault(); alert('Thank you! We will reach out soon.'); this.reset();
  });
</script>

</body>
</html>
